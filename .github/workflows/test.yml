name: Test

on: 
  push:
    branches:
      - '**'
  pull_request:
      types: [closed]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Test Action
      uses: ./
      with:
          releaseBranches: master,alpha,beta,rc
